---
description: Premium implementation plan at minimal Fable cost - repo type map + free Sol scout + one turn-budgeted Claude Fable 5 call (runs the fable-plan skill)
---
Load the `fable-plan` skill with the skill tool and execute it exactly as written
for the following request (flags like `--plan-turns=`, `--manifest`, `--out=` may
be embedded in it; the skill parses them).

REQUEST: $ARGUMENTS
